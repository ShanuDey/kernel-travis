language: C
sudo: required

before_install:
  - sudo apt-get update
  - sudo apt-get install -y git zip 
before_script:
  - cd $HOME && PATH=~/bin:$PATH
  - curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
  - chmod a+x ~/bin/repo
script:
  - cd $HOME && git clone -b 10 https://github.com/iamsaalim/kernel_xiaomi_laurel_sprout kernel
  - cd $HOME/kernel 
  - git push -q https://$GITHUB_TOKEN@github.com/stormbreaker-project/kernel_laurel_sprout HEAD:ten
